## 🐍 Estudo de módulos do Python

Este repositório é dedicado ao **estudo e exploração de alguns dos módulos mais úteis e comuns da biblioteca padrão do Python**. Ele serve como um recurso prático para entender a funcionalidade e a aplicação de cada módulo através de exemplos de código e explicações concisas.

---

## 📚 Módulos Incluídos

Os seguintes módulos estão sendo explorados neste projeto, com arquivos e pastas dedicados a cada um:

| Módulo | Descrição Principal | Tópicos de Estudo (Exemplos) |
| :--- | :--- | :--- |
| **`math`** | Funções matemáticas de ponto flutuante. | Funções trigonométricas, constantes ($\pi$, $e$), logaritmos, raiz quadrada. |
| **`os`** | Interface com o sistema operacional. | Manipulação de caminhos de arquivo, variáveis de ambiente, comandos do sistema. |
| **`statistics`** | Funções para cálculo de estatística matemática. | Média, mediana, moda, desvio padrão, variância. |
| **`re`** | Operações com **Expressões Regulares (Regex)**. | Busca, substituição, divisão e compilação de padrões. |
| **`hashlib`** | Geração de **hashes** criptográficos. | Algoritmos SHA256, MD5, e uso para verificação de integridade de dados. |
| **`collections`** | Tipos de contêineres especializados de alto desempenho. | `namedtuple`, `defaultdict`, `Counter`, `deque`. |
| **`random`** | Geração de números pseudo-aleatórios. | Geração de inteiros, seleção aleatória de listas, embaralhamento (`shuffle`). |
| **`tkinter`** | Biblioteca padrão para a criação de **interfaces gráficas de usuário (GUI)**. | Criação de janelas, botões, *labels*, caixas de texto (*entry*), eventos. |

---

## 🎯 Objetivo do Repositório

* **Aprender Fazendo:** Fornecer exemplos de código **executáveis e comentados** para cada funcionalidade do módulo.
* **Referência Rápida:** Servir como um guia de referência rápido sobre como usar as principais funções desses módulos.
* **Melhores Práticas:** Demonstrar o uso idiomático e eficiente dos recursos da biblioteca padrão.

---

## 🚀 Como Usar

1.  **Clone o repositório:**
    ```bash
    git clone [https://github.com/ViitoDev/ModulosPython.git](https://github.com/ViitoDev/ModulosPython.git)
    ```
2.  **Navegue até o módulo de interesse:**
    ```bash
    cd ModulosPython/1-os.py/
    # ou
    cd ModulosPython/2-math.py/
    ```
3.  **Execute os exemplos:**
    ```bash
    python 1-os.py
    ```

---

## 🤝 Contribuições

Contribuições são bem-vindas! Se você tiver um exemplo mais claro, uma aplicação interessante ou identificar um erro, sinta-se à vontade para:

1.  Fazer um **fork** do projeto.
2.  Criar uma **branch** para a sua funcionalidade (`git checkout -b feature/AmazingFeature`).
3.  Fazer o **commit** das suas mudanças (`git commit -m 'Add some AmazingFeature'`).
4.  Fazer o **push** para a branch (`git push origin feature/AmazingFeature`).
5.  Abrir um **Pull Request**.


